# Projeto confere placa
estou usando um dicionário para pesquisar as placas, mas a quantidade de placas não justifica o trabalho que dá
acho melhor usar uma lista com tudo e separando os elementos
Colegas reclamaram da linguagem utilizada, como o botão "Abrir CSV" melhor mudar para "Carregar Dados" para melhorar a intuitividade

lista = [["PLACA","NOME","STATUS","TIMESTAMP"]]

import csv, 

def load(path) 
def save(path,DATA)
def delete(DATA,PLACA)
def find(STR)
